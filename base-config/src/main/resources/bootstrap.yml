spring:
  application:
    name: config-server #Eureka注册名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jackvcqin/springcloud.git   #配置文件所在仓库
          username: jackvcqin
          password: qinfuxin1986
          default-label: master   #配置文件分支
          search-paths: base-config/config  #配置文件所在根目录