eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/    #注册中心地址
logging:
  file:
    path: E:/logs/pay   #日志文件路径
