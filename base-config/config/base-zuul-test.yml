eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
  client:
    service-url:
      defaultZone: http://192.168.231.135:9001/eureka/    #注册中心地址
logging:
  file:
    path: /home/javaweb/logs/zuul   #日志文件路径
